<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Tabavoco.MiniVolumeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Background="#E0000000" Padding="8" RightTapped="OnRightTapped" 
          PointerPressed="OnPointerPressed" PointerMoved="OnPointerMoved" PointerReleased="OnPointerReleased">
        <Grid.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="Exit" Click="OnExitClicked"/>
            </MenuFlyout>
        </Grid.ContextFlyout>
        
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="🔊" 
                       VerticalAlignment="Center" 
                       FontSize="16" 
                       Foreground="White"/>
            <Slider x:Name="VolumeSlider"
                    Width="200"
                    Minimum="0"
                    Maximum="100"
                    Value="50"
                    VerticalAlignment="Center"
                    ValueChanged="OnVolumeChanged"/>
            <TextBlock x:Name="VolumeLabel"
                       Text="50%"
                       VerticalAlignment="Center"
                       FontSize="12"
                       Foreground="White"
                       Width="30"/>
        </StackPanel>
    </Grid>
</Window>